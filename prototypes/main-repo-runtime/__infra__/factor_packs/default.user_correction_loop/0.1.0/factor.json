{
  "schema_version": "factor.v0",
  "id": "default.user_correction_loop",
  "name": "user-correction-loop",
  "framework_id": "agent_session_review.v0",
  "stage": "signal_extraction",
  "runtime_profile": "default",
  "default_enabled": true,
  "version": "0.1.0",
  "status": "active",
  "description": "Detects repeated user correction turns in one session.",
  "entrypoint": "factor:UserCorrectionLoopFactor",
  "inputs": ["session.events", "user_turn.content"],
  "outputs": ["tag", "score", "evidence_ref", "verdict_signal"],
  "permissions": ["read_session_events"],
  "risks": ["may treat intentional iterative refinement as correction loop"],
  "rollback": "delete this factor folder or disable it in local config",
  "runtime": {
    "mode": "in_process",
    "timeout_ms": 1000
  }
}
