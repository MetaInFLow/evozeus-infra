{
  "schema_version": "factor.v0",
  "id": "default.same_target_rework",
  "name": "same-target-rework",
  "framework_id": "agent_session_review.v0",
  "stage": "signal_extraction",
  "runtime_profile": "default",
  "default_enabled": true,
  "version": "0.1.0",
  "status": "active",
  "description": "Detects repeated user turns targeting the same task or object.",
  "entrypoint": "factor:SameTargetReworkFactor",
  "inputs": ["session.events", "user_turn.content"],
  "outputs": ["tag", "evidence_ref", "verdict_signal"],
  "permissions": ["read_session_events"],
  "risks": ["may classify normal iterative work as rework"],
  "rollback": "delete this factor folder or disable it in local config",
  "runtime": {
    "mode": "in_process",
    "timeout_ms": 1000
  },
  "run": {
    "mode": "sync",
    "timeout_ms": 1000
  }
}
